spring.application.name=portfolio-backend
server.port=8080

# H2 Database
spring.datasource.url=jdbc:h2:mem:portfoliodb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# File storage
app.upload.dir=${user.dir}/uploads

# RESEND API KEY
resend.api.key=${RESEND_API_KEY}

# Contact email destination
app.contact.notify-email=adarshr3131@gmail.com

# GitHub
app.github.username=theadarshh

# LeetCode
app.leetcode.username=theadarshh